# API change brief

AIDLC_DRAFT — proposed `change` contract; execution and approval are NOT_RUN.

## Request and bounded impact

Reference the request, current API behavior and repository evidence. State the proposed behavior, explicit exclusions, affected local consumers and reason the change is low risk. Record adopted/rejected knowledge references and applicability when available.

## Acceptance and gate designs

| Request FR/NFR | AC ID or approved exclusion | Observable Oracle | Check and raw evidence required |
|---|---|---|---|

List both Architecture and Quality Gate IDs, rule versions, scope, executable commands, cwd, success thresholds, expected report paths and approved installation plans for missing rules. No empty, manual-only or zero-test substitute qualifies.

## Implementation authority to request

List exact writable paths, commands and side effects, ordered steps, check dependencies and rollback. Record unresolved assumptions and scope exclusions requiring approval. This section proposes authority; the separate valid approval grants it.

## Routing evidence

`needs_review`: unresolved; replace with a boolean only after examining evidence. Explain whether multiple local consumers or coverage complexity requires the independent compatibility and tests reviews. Return the same typed fact in the stage result.

Record security-sensitive and other escalation checks separately. A routing choice never clears a risk flag or grants permission. Unknown risk or unknown routing facts block progress.
